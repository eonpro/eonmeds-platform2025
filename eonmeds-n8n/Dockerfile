FROM n8nio/n8n:latest

# Set the user to run n8n
USER root

# Install additional dependencies if needed
# RUN apk add --no-cache python3 py3-pip

# Switch back to the node user
USER node

# Expose the n8n port
EXPOSE 5678

# The entrypoint is already set in the base image
